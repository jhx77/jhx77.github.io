export type Project = {
  slug: string;
  title: string;
  subtitle: string;
  period: string;
  role: string;
  keywords: string[];
  narrative: string;
  engineeringLens: {
    title: string;
    body: string;
  }[];
};

export const projects: Project[] = [
  {
    slug: "fastkb",
    title: "FastKB 企业私有 AI 平台",
    subtitle: "面向企业知识问答、智能体编排与工具调用的私有化 AI 应用底座",
    period: "2025",
    role: "后端与 AI 应用工程设计",
    keywords: [
      "RAG",
      "pgvector",
      "PostgreSQL 全文检索",
      "QueryRewrite",
      "Workflow",
      "RBAC",
      "MCP Tool"
    ],
    narrative:
      "FastKB 的核心场景不是做一个简单的聊天入口，而是把企业内部的文档、制度、业务系统和智能体能力接入到一个可以私有化部署的 AI 平台里。这个场景真正困难的地方在于，企业知识问答追求的不是一次回答看起来流畅，而是回答必须可追溯、权限必须可隔离、多轮对话不能跑偏，并且不同模型厂商之间要能够被替换。早期如果只依赖单一路径的向量召回，系统会很容易出现 RAG 效果不稳定的问题：用户问题的表达和文档原文不一致时，向量相似度可能召回语义接近但事实不准的片段；而一些编号、产品名、流程名、岗位名又更适合关键词匹配。我的设计思路是把检索链路拆成可观测、可调权重的多阶段流程，而不是把所有质量压力都压在 embedding 上。\n\n文档进入系统后，先经过上传、Tika 解析、内容清洗和结构化抽取，再按照标题层级、段落边界和 token 长度做分块。分块并不是单纯按字符切开，而是尽量保留语义完整性，避免一个制度条款被切成上下文缺失的碎片。随后每个 chunk 会进入向量化流程并写入 pgvector，用于语义召回；同时把标题、正文、标签和来源信息写入 PostgreSQL 全文检索索引，用于精确词、专有名词和业务术语召回。查询侧先经过 QueryRewrite，把用户的口语化问题改写成更适合检索的表达，例如补全省略主语、拆解复合问题、保留关键实体，再并行走 pgvector 向量检索和 PostgreSQL 全文检索。混合检索的关键取舍在于：向量检索负责理解意图，全文检索负责守住事实锚点，两者结果再根据相似度、关键词命中、知识库权重、更新时间和权限过滤进行融合。这样做虽然链路比单向量方案更长，但它把召回错误从不可解释的黑盒问题，变成了可以观察、可以调参、可以针对业务语料优化的工程问题。\n\n在智能体执行层，FastKB 采用 Workflow 的 Graph 编排模型来组织复杂任务。每个流程由节点、边和状态流构成：节点负责执行一个明确动作，例如检索知识、调用模型、触发 MCP Tool、写入记忆或进行条件判断；边描述节点之间的流转关系；状态流携带用户输入、检索结果、中间判断、工具返回值和最终回答。这个设计的价值是把智能体从一段不可控的 prompt 变成可拆解的执行图。当后续需要加入新的工具节点、审批节点或知识库检索节点时，只要遵守统一的输入输出协议，就可以扩展能力，而不需要改动整条链路。对于企业场景，这种可扩展节点设计比一次性写死流程更重要，因为不同部门对知识问答、数据查询、表单生成和外部接口调用的边界完全不同。\n\n多轮对话的难点则来自上下文膨胀和历史信息污染。FastKB 的 AI 记忆采用滑动窗口 + 摘要机制：最近若干轮对话保留原文，保证模型能理解当前语境；更早的对话被压缩成结构化摘要，记录用户目标、已确认事实、未完成任务和偏好约束。摘要不是为了让系统显得更智能，而是为了控制 token 成本并降低无关历史对当前回答的干扰。当上下文超过阈值时，系统优先保留强相关问题、工具结果和用户显式确认的信息，把闲聊或过期推断移出窗口。这样既能维持连续对话体验，也让模型调用成本更可控。\n\n权限系统采用 RBAC 作为基础模型，但没有停留在角色名称上，而是把用户、部门、岗位和知识资源之间的绑定关系纳入授权判断。企业知识库里同一份文档可能只允许某个部门查看，某个岗位可编辑，另一个角色只能引用结果而不能下载原文。因此权限过滤必须前置到检索阶段，而不是等模型生成后再做遮挡。用户发起查询时，系统先根据用户身份解析部门、岗位和角色，再得到可访问知识库与文档范围，检索阶段只在授权集合内召回片段。这样设计的原因很直接：RAG 如果先召回再过滤，未授权内容已经进入上下文，权限隔离就失去了意义。\n\n模型策略层面，我把多厂商模型抽象成统一调用入口，屏蔽不同厂商在 chat、embedding、stream、tool calling 和错误码上的差异。业务层只关心模型能力类型、上下文长度、成本和可用性，不直接绑定具体供应商。这样在私有化部署时可以接入本地模型，在成本敏感场景使用更便宜的模型，在高质量回答场景切换更强模型。MCP Tool 则用于扩展 AI 的能力边界，把外部工具调用纳入可审计的协议中。AI 不再只回答知识库内容，还可以在受控范围内查询业务系统、触发流程或调用内部服务。整个项目最终解决的不是某一个单点功能，而是把企业 AI 应用里最容易失控的检索、记忆、权限、模型和工具调用重新组织成一套可治理的工程架构。",
    engineeringLens: [
      {
        title: "检索取舍",
        body:
          "单向量方案实现最快，但面对企业语料时可解释性不足。混合检索用更长的链路换来可调权重、可定位误召回原因和更稳定的事实锚点。"
      },
      {
        title: "执行模型",
        body:
          "Workflow Graph 把智能体执行拆成节点、边和状态流，降低 prompt 黑盒风险，也为后续接入审批、工具和业务系统留下扩展点。"
      },
      {
        title: "权限前置",
        body:
          "权限过滤必须在检索前完成，避免未授权片段进入模型上下文。RBAC 叠加部门和岗位绑定，使企业内部知识边界更清晰。"
      }
    ]
  },
  {
    slug: "smart-life",
    title: "智慧生活平台",
    subtitle: "生活服务、秒杀交易、缓存治理与 AI 客服结合的后端系统优化实践",
    period: "2024",
    role: "Java 后端工程与性能优化",
    keywords: [
      "Spring Boot",
      "Redis",
      "Lua",
      "Kafka",
      "Caffeine + Redis",
      "订单一致性",
      "LangChain4j"
    ],
    narrative:
      "智慧生活平台的业务形态接近日常服务聚合：用户浏览服务、领取优惠、参与秒杀、下单支付，并在售后环节通过客服获得帮助。系统最初的挑战不是页面能不能展示，而是交易链路在高并发下是否还能保持稳定。秒杀活动会把大量请求集中到极短时间窗口，如果所有请求都直接落到数据库，库存行会成为争抢热点，数据库连接池和行锁都会被放大成瓶颈；同时，热门服务详情页会反复击穿缓存，一旦缓存失效，后端会在同一时刻承受查询洪峰。我的优化目标是把数据库从流量峰值中心移出来，让系统先在内存和消息队列层吸收压力，再用可恢复的方式落库。\n\n秒杀链路采用 Redis 预扣库存作为第一道闸门，并使用 Lua 脚本把库存扣减和一人一单判断放在同一个原子执行单元里。这样设计是为了避免先查库存再扣减之间出现竞态，也避免同一个用户在并发点击时生成多笔订单。Lua 脚本只处理高频、短路径的判断：活动是否存在、库存是否大于零、用户是否已经下单。如果判断通过，系统先写入用户下单标记并预扣库存，然后把订单创建请求投递到 Kafka。Kafka 的作用是异步削峰，它把突发流量从同步 HTTP 请求链路中移开，让用户请求快速得到排队结果，下游订单服务再按照可控速率消费。削峰的意义不是让消息队列显得高级，而是保护数据库、支付和库存持久化这些更昂贵的环节不被瞬时流量击穿。\n\n缓存治理上，我区分了逻辑过期和直接过期。对于普通、低热度数据，直接设置 TTL 足够简单；但对秒杀商品、热门服务详情这类高热度数据，如果直接过期，过期瞬间会引发大量请求同时回源。逻辑过期的做法是缓存值里保存业务数据和过期时间，请求读到逻辑过期数据时仍可先返回旧值，同时通过互斥锁触发后台重建缓存。再结合 Caffeine + Redis 的多级缓存，本地 Caffeine 承接短时间内的重复读，Redis 作为跨实例共享缓存，数据库只承担最终数据源职责。这个结构让系统在热点读场景下更快，也让缓存失效从同步阻塞变成后台修复。\n\n一致性问题集中在库存、订单和支付三处。Redis 预扣库存提升了抗压能力，但最终库存必须和数据库一致，因此订单消费者需要具备幂等能力，消费失败时可以重试，超过重试边界再进入补偿流程。数据库侧通过唯一索引约束一人一单，防止消息重复或极端并发绕过业务判断。支付环节则需要处理重复回调、用户重复点击和订单状态竞争，状态流转必须从待支付到已支付或已关闭单向推进，不能因为并发回调把关闭订单重新改成支付成功。定时任务负责扫描超时未支付订单，自动关闭订单并释放库存或触发补偿，避免库存长期悬挂。客服部分接入 LangChain4j 和 Function Calling，把订单查询、服务说明、退款规则等动作封装成可调用函数，让 AI 客服不只生成自然语言，还能在授权范围内读取业务状态。整体优化后，系统的关键变化是同步交易路径变短，热点读压力被多级缓存吸收，写入洪峰被 Kafka 平滑化，库存一致性通过幂等、唯一约束和补偿机制共同兜底。",
    engineeringLens: [
      {
        title: "抗压策略",
        body:
          "把秒杀请求拆成 Redis 原子校验、Kafka 排队和数据库异步落单三段，让核心数据库远离瞬时洪峰。"
      },
      {
        title: "缓存策略",
        body:
          "低热度数据直接过期，高热度数据逻辑过期；Caffeine 负责进程内重复读，Redis 负责跨实例共享。"
      },
      {
        title: "一致性边界",
        body:
          "库存、订单和支付不追求单点魔法，而是用 Lua 原子性、唯一索引、消费者幂等和超时补偿共同收敛状态。"
      }
    ]
  }
];
